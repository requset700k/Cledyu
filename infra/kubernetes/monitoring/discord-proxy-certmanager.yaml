apiVersion: apps/v1
kind: Deployment
metadata:
  name: alertmanager-discord-proxy-certmanager
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alertmanager-discord-proxy-certmanager
  template:
    metadata:
      labels:
        app: alertmanager-discord-proxy-certmanager
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: discord-proxy
          image: rogerrum/alertmanager-discord:1.0.4
          env:
            - name: DISCORD_WEBHOOK
              valueFrom:
                secretKeyRef:
                  name: alertmanager-discord-webhook-certmanager
                  key: webhook-url
            - name: DISCORD_USERNAME
              value: "Alertmanager"
          ports:
            - containerPort: 9094
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: alertmanager-discord-proxy-certmanager
  namespace: monitoring
spec:
  selector:
    app: alertmanager-discord-proxy-certmanager
  ports:
    - port: 9094
      targetPort: 9094
