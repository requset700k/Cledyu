<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.cledyu.keycloak</groupId>
  <artifactId>naver-idp</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <name>Cledyu Keycloak Naver Identity Provider</name>
  <description>네이버 소셜 로그인 브로커 SPI. 네이버는 OIDC(id_token)를 발급하지 않아 빌트인 oidc
    프로바이더로는 동작하지 않으므로, GitHub 빌트인과 동일하게 AbstractOAuth2IdentityProvider 를
    상속해 access_token + userinfo(https://openapi.naver.com/v1/nid/me) 기반으로 매핑한다.</description>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <keycloak.version>26.6.1</keycloak.version>
  </properties>

  <dependencies>
    <!-- Keycloak 런타임이 제공한다(provided): JAR 에 포함하지 않는다. -->
    <dependency>
      <groupId>org.keycloak</groupId>
      <artifactId>keycloak-core</artifactId>
      <version>${keycloak.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.keycloak</groupId>
      <artifactId>keycloak-server-spi</artifactId>
      <version>${keycloak.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.keycloak</groupId>
      <artifactId>keycloak-server-spi-private</artifactId>
      <version>${keycloak.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.keycloak</groupId>
      <artifactId>keycloak-services</artifactId>
      <version>${keycloak.version}</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>naver-idp</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
    </plugins>
  </build>
</project>
