## Keycloak 파드 SA. CR podTemplate.serviceAccountName 이 참조(온프렘 keycloak_foundation 과 동일 이름).
## AWS 접근 불필요 → IRSA annotation 없음(평범한 SA). 온프렘은 별도 SA 매니페스트로 생성했다.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccountName }}
  namespace: {{ .Release.Namespace }}
