apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: data-chromadb
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: default
  source:
    repoURL: https://github.com/requset700k/Cledyu.git
    targetRevision: main
    path: gitops/apps/chromadb
    helm:
      releaseName: chromadb
  destination:
    server: https://kubernetes.default.svc
    namespace: ai-tutor

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
