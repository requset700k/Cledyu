---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-external-secrets
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - repoURL: https://charts.external-secrets.io
      chart: external-secrets
      targetRevision: 0.14.3
      helm:
        releaseName: external-secrets
        valueFiles:
          - $values/gitops/apps/external-secrets/values.yaml
    - repoURL: https://github.com/requset700k/Cledyu.git
      targetRevision: main
      ref: values
    - repoURL: https://github.com/requset700k/Cledyu.git
      targetRevision: main
      path: infra/kubernetes/external-secrets
      directory:
        include: "*.yaml"
        # eso-target-namespaces.yaml 은 EKS DR 선행 namespace 전용(eks-data-eso-store 만 include).
        # 온프렘은 service-api/-validation-engine 차트가 그 namespace 를 소유하므로, 온프렘 이 앱이
        # 같이 만들면 두 Application 간 소유권 충돌이 난다(Codex P2) → 온프렘에선 제외.
        exclude: "eso-target-namespaces.yaml"
  destination:
    server: https://kubernetes.default.svc
    namespace: external-secrets
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
  # ESO 가 reconcile 시 채우는 metadata(finalizers 등)로 인해 ExternalSecret 이
  # 만성 OutOfSync → self-heal 무한 반복하던 문제를 무시 처리.
  ignoreDifferences:
    - group: external-secrets.io
      kind: ExternalSecret
      jsonPointers:
        - /metadata/finalizers
