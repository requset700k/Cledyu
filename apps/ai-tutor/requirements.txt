# 런타임 의존성 — Dockerfile 에서 설치. 테스트 전용 의존은 requirements-dev.txt.
# fastapi 는 chromadb 자체 핀과의 충돌을 피해 범위 핀으로 두고 pip resolver 에 맡긴다.
# starlette 하한은 CVE-2025-62727(Range 헤더 DoS, < 0.49.1) 차단용.
fastapi>=0.118,<1
starlette>=0.49.1
uvicorn[standard]>=0.30,<1
pydantic-settings>=2.7,<3
google-genai>=1.16,<2
prometheus-client>=0.21,<1
# RAG(선택) — HttpClient + 기본 ONNX 임베딩(all-MiniLM-L6-v2, 기획서의 임베딩 모델과 동일).
chromadb>=1.0.8,<2
# Rate limit 공유 카운터(선택) — AI_TUTOR_REDIS_URL 설정 시에만 사용.
redis>=5,<6
